language: php
sudo: false
php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - nightly
  - hhvm
matrix:
  allow_failures:
    - php: "hhvm"
    - php: "nightly"
before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction --dev
script: ./vendor/bin/phpunit
